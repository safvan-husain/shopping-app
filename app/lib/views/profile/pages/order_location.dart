import 'package:flutter/material.dart';

class OrderLocationView extends StatelessWidget {
  const OrderLocationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
